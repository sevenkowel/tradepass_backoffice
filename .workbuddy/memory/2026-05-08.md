# 2026-05-08

## 工作概要
- **CRM 代码优化**: 租户清理、Mock Service 层提取、Store 重写、性能/类型修复
- **Dev Server 3003 修复**: 移除 turbopack/polling，改用 `--webpack` 模式启动
- **Bug 修复**: `department.service.ts` mock-departments 路径错误
- **KYC 重构方案**: 综合 6 份文档输出 `docs/KYC-COMPLETE-REDESIGN.md`（6步流程+9地区+5Phase实施）
- **CRM 菜单重构 v2**: 从13组调整为14组，配置去中心化，输出 `docs/CRM-MENU-STRUCTURE.md`
- **Clients 模块 Phase 1**: 类型扩展 + Client Service + 7个页面（列表/Profile/12Tab/Tags/Segments/Lifecycle/Notes/Relationships），输出 `docs/CLIENTS-MODULE-PLAN.md`
- **CRM Client List 表格优化**:
  - 删除 Export 按钮/区域
  - 增加服务端分页（page/pageSize/total 状态 + 分页 UI）
  - FilterBar 增加 DateRangeFilter 组件（daterange 类型：两个 date input 并排 + "to" 分隔）
  - FilterBar 增加 Apply 按钮，组合筛选点击后统一执行
  - 筛选区域文案全部改为英文
- **CRM Client Detail Page 重构（PRD v1.0）**:
  - 输出需求文档 `docs/PRD-Client-Detail-v1.0.md`，经用户确认6个问题后执行
  - 新增类型定义 `src/types/backoffice/client-detail.ts`（17个数据模型）
  - 新增 mock 数据 `src/lib/crm/mock-client-detail.ts`
  - 扩展 `clientService.getDetail()` 聚合所有 Tab 数据
  - 14 个 Tab 全部实现（Overview/KYC/Accounts/Funds/Trading/Risk/Devices/Cases/Tickets/Permissions/Agreements/Timeline/Notes/Logs）
  - 新增 `ClientDetailHeader.tsx`（Sticky Header：基础信息 + 状态徽章 + 快捷操作）
  - 重写 `page.tsx`（14 Tab 路由 + Header + Tab 导航）
  - 增强 `layout.tsx` Sidebar（Risk Score Card / Lifecycle Stage / User Value Info）
  - Risk Tab 集成 ECharts 关系图谱（共享 IP/设备/银行卡关联网络）
  - KYC Tab 左图右文 OCR 对比占位 + 审核操作按钮
  - 新增系统级 hooks：`useAuditTrail.ts` / `useRBAC.ts` / `useTimelineIntegration.ts`
  - 安装 echarts 依赖
- **CRM KYC Center 改造计划**: 结合 CLM Center 产品方案 v1.0.1，输出实施文档 `docs/CRM-KYC-CENTER-PLAN.md`
  - 现状痛点分析：KYCTab 简陋、kyc.service 能力薄、mock 数据模型不匹配、菜单混排
  - Gap 总表：Operations 7 模块 + Configuration 6 模块，按 Phase 1/2/3 分优先级
  - 信息架构重组：`/crm/clm/*` 新路由体系，Operations/Configuration 清晰隔离
  - 数据模型设计：`CLMCase` / `CLMCustomerSnapshot` / `CLMAuditLog` / `RiskAssessment` 等
  - 页面级设计：Workspace + Review Queue + Case Detail（含 Decision Panel/Comments/Timeline）
  - Service 层架构：`case/queue/customer/sla/audit/workspace` 5 个 service
  - 实施路线图：Phase 1（核心工作流）→ Phase 2（扩展运营）→ Phase 3（配置模块）
  - 10 项关键设计决策（路由前缀、数据存储、KYCTab 保留策略等）
- **CRM CLM Center Phase 1 实现（核心工作流）**: 已完成
  - 新增类型：`src/types/clm/`（case.ts / customer.ts / audit.ts / workspace.ts）
  - 新增 mock：`src/lib/clm/mock/`（20 条 Case + Workspace KPI + Risk Alerts + Audit Logs）
  - 新增 Service：`src/lib/clm/services/`（case.service / workspace.service / audit.service）
  - 新增 UI 组件：`src/components/crm/ui/`（SLABadge / CaseTypeBadge / AMLStatusBadge）
  - 新增 CLM 组件：`src/components/crm/clm/`（DecisionPanel / CaseTimeline / DocumentPreview / RiskAssessmentPanel / CommentPanel）
  - Workspace 页面：`/crm/clm/workspace`（KPI Cards + Queue Summary + Risk Alerts + My Tasks 5 Tab + Recent Activity）
  - Review Queue 页面：`/crm/clm/review-queue`（Filter Bar + EnhancedDataTable + Batch Assign + 智能排序 + 分页）
  - Case Detail 页面：`/crm/clm/cases/[id]`（Case Header + Customer Snapshot + Materials + Risk Assessment + Decision Panel + Comments + Timeline）
  - Cases 列表页：`/crm/clm/cases`（全量 Case 列表）
  - Sidebar 菜单：`KYC Center` → `CLM Center`，12 个子项（Operations 5 + Configuration 6）
  - Dev server 3003 启动正常，无编译错误
- **CRM CLM Center Phase 2 实现（扩展运营）**: 已完成
  - SLA & Monitoring 页面：`/crm/clm/sla-monitoring`（5 KPI Cards + Queue Backlog 条形图 + Automation Health 6 指标 + Reviewer Performance 表格 + Alerts 列表）
  - Audit Trail 页面：`/crm/clm/audit-trail`（审计日志表格 + Action/TargetType 筛选 + 变更前后值对比 + 只读提示 + 分页）
- **CRM CLM Center Phase 3 实现（配置模块）**: 已完成
  - KYC Policies：`/crm/clm/policies`（5 条策略列表 + IF/THEN 规则展示 + 类型/状态标签 + 展开详情）
  - KYC Levels：`/crm/clm/levels`（5 个 Tier 卡片 + 11x5 权限矩阵表格 Yes/Limited/No）
  - Forms & Fields：`/crm/clm/forms`（6 个表单列表 + 字段数/国家/状态）
  - Compliance Templates：`/crm/clm/templates`（4 个监管模板 + Policies/Forms/Agreements 统计）
  - Agreements：`/crm/clm/agreements`（4 条协议 + 版本/语言/签署数/Force Re-sign 标签）
  - Workflow Settings：`/crm/clm/workflows`（5 个工作流规则 + Routing/Escalation/SLA/Automation/Reopen 类型）
  - **CLM Center 全部 12 个页面实现完毕**，Operations 6 + Configuration 6
