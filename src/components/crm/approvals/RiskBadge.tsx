"use client";

// Approval Center re-exports the canonical CRM RiskBadge so existing
// import paths inside this folder continue to work. New code should
// import from `@/components/crm/ui` directly.
export { RiskBadge } from "@/components/crm/ui/RiskBadge";
